# 图表生成API接口文档

## 服务概述

**服务名称**: 智能图表生成API
**服务描述**: 基于SQL查询结果数据，自动分析数据结构并智能选择最适合的图表类型配置
**API版本**: v1.0.0
**技术栈**: FastAPI + Python
**服务端口**: 12345 (默认)

---

## API端点

### 主要接口

| 方法 | 路径 | 描述 |
|------|------|------|
| `POST` | `/generate-chart` | 根据查询结果生成图表配置 |
| `GET` | `/` | 获取API服务信息 |
| `GET` | `/docs` | Swagger交互式API文档 |
| `GET` | `/openapi.json` | OpenAPI规范文档 |

---

## 核心接口详情

### POST /generate-chart

#### 接口描述
根据SQL查询结果数据自动生成图表配置，包括图表类型选择、坐标轴配置、数据映射等。

#### 请求信息
- **URL**: `http://localhost:12345/generate-chart`
- **方法**: `POST`
- **Content-Type**: `application/json`

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 | 示例 |
|--------|------|------|------|------|
| `query_result` | `Array[Object]` | 是 | SQL查询结果数据（JSON对象数组） | `[{"department": "技术部", "count": 50}]` |

#### 请求格式

```json
{
  "query_result": [
    {
      "字段名1": "值1",
      "字段名2": 数值2,
      "字段名3": "值3"
    }
  ]
}
```

#### 响应格式

**成功响应 (200 OK)**

| 字段名 | 类型 | 描述 | 示例 |
|--------|------|------|------|
| `type` | `string` | 图表类型 | `"bar"`, `"area"`, `"pie"`, `"radial"`, `"stat"` |
| `data` | `Array[Object]` | 处理后的图表数据 | `[{"x": "技术部", "y": 50}]` |
| `xKey` | `string \| null` | X轴数据字段名 | `"department"` |
| `yKey` | `string \| null` | Y轴数据字段名 | `"count"` |
| `nameKey` | `string \| null` | 名称字段（饼图/径向图） | `"browser"` |
| `valueKey` | `string \| null` | 数值字段（饼图/径向图） | `"visitors"` |
| `xAxisLabel` | `string \| null` | X轴显示标签 | `"部门"` |
| `yAxisLabel` | `string \| null` | Y轴显示标签 | `"人数"` |
| `supportedTypes` | `Array[string] \| null` | 支持切换的图表类型 | `["bar", "area"]` |

---

## 图表类型智能检测算法

### 检测优先级

1. **数字卡片 (stat)** - 单行数据
2. **饼图/径向图 (pie/radial)** - 2列分类数据
3. **面积图 (area)** - 包含时间序列数据
4. **柱状图 (bar)** - 分类对比数据
5. **默认回退** - 柱状图或数字卡片

### 详细检测逻辑

#### 1. 数字卡片 (stat)
**检测条件**:
- 数据行数 = 1
- 任意列数

**适用场景**:
- 聚合统计结果 (COUNT, SUM, AVG)
- 单个KPI指标
- 多个KPI指标面板

**响应示例**:
```json
{
  "type": "stat",
  "data": [{"total_revenue": 1285000}],
  "supportedTypes": null
}
```

#### 2. 饼图/径向图 (pie/radial)
**检测条件**:
- 数据行数 ≥ 2
- 列数 = 2
- 包含1个分类字段 + 1个数值字段

**适用场景**:
- 占比分析
- 分类统计
- 部门分布等

**响应示例**:
```json
{
  "type": "pie",
  "data": [
    {"browser": "Chrome", "visitors": 275},
    {"browser": "Safari", "visitors": 200},
    {"browser": "其他", "visitors": 108}
  ],
  "nameKey": "browser",
  "valueKey": "visitors",
  "supportedTypes": ["pie", "radial"]
}
```

#### 3. 面积图 (area)
**检测条件**:
- 包含日期字段 + 数值字段
- 适合时间序列数据展示

**适用场景**:
- 趋势分析
- 时间序列数据
- 增长变化展示

**响应示例**:
```json
{
  "type": "area",
  "data": [
    {"month": "2024-01", "sales": 150000},
    {"month": "2024-02", "sales": 180000}
  ],
  "xKey": "month",
  "yKey": "sales",
  "xAxisLabel": "月份",
  "yAxisLabel": "销售额",
  "supportedTypes": ["area", "bar"]
}
```

#### 4. 柱状图 (bar)
**检测条件**:
- 多行数据 (≥ 2行)
- 包含分类字段 + 数值字段
- 默认回退选项

**适用场景**:
- 分类对比
- 排名展示
- 数量比较

**响应示例**:
```json
{
  "type": "bar",
  "data": [
    {"department": "技术部", "employee_count": 186},
    {"department": "产品部", "employee_count": 125}
  ],
  "xKey": "department",
  "yKey": "employee_count",
  "xAxisLabel": "部门",
  "yAxisLabel": "员工数量",
  "supportedTypes": ["bar", "area"]
}
```

---

## 数据类型智能识别

### 支持的字段类型

| 类型 | 识别规则 | 示例字段 |
|------|---------|---------|
| **数值字段** | 整数、浮点数、可转换为数字的字符串 | `count`, `amount`, `price`, `revenue` |
| **日期字段** | 标准日期格式 (YYYY-MM-DD, YYYY/MM/DD等) | `created_at`, `date`, `month`, `updated_at` |
| **文本字段** | 字符串且不符合数值和日期格式 | `name`, `department`, `category`, `status` |

### 支持的日期格式

- `YYYY-MM-DD` (2024-01-15)
- `YYYY/MM/DD` (2024/01/15)
- `MM-DD-YYYY` (01-15-2024)
- `MM/DD/YYYY` (01/15/2024)
- `YYYY-MM-DD HH:MM:SS` (2024-01-15 14:30:00)
- `YYYY-MM-DDTHH:MM:SS` (2024-01-15T14:30:00)
- `YYYY-MM` (2024-01)
- `MM-DD` (01-15)

---

## 智能标签映射

### 常见字段映射表

| 英文字段 | 中文标签 | 说明 |
|---------|---------|------|
| `department` | 部门 | 部门名称 |
| `count`, `num`, `quantity` | 数量 | 数量统计 |
| `employee_count` | 员工数量 | 员工人数 |
| `total`, `sum` | 总计 | 总和统计 |
| `average`, `avg` | 平均值, 平均 | 平均数 |
| `max`, `maximum` | 最大值 | 最大数值 |
| `min`, `minimum` | 最小值 | 最小数值 |
| `sales`, `revenue` | 销售额, 收入 | 销售相关 |
| `price`, `amount` | 价格, 金额 | 金额相关 |
| `profit` | 利润 | 利润数值 |
| `cost` | 成本 | 成本数值 |
| `created_at`, `date`, `time` | 创建日期, 日期 | 时间相关 |
| `name`, `title` | 名称, 标题 | 名称相关 |
| `category`, `type`, `group` | 分类, 类型, 分组 | 分类相关 |
| `status` | 状态 | 状态信息 |
| `browser` | 浏览器 | 浏览器名称 |
| `visitors`, `users` | 访问量, 用户数 | 用户相关 |
| `region`, `city`, `country` | 地区, 城市, 国家 | 地理位置 |

---

## 数据处理优化

### 数据量限制

| 图表类型 | 最大显示项数 | 超出处理 |
|---------|-------------|---------|
| **饼图/径向图** | 8项 | 超出项合并为"其他" |
| **柱状图/面积图** | 20项 | 只显示前20项 |

### 数据处理规则

1. **饼图数据优化**:
   - 按数值降序排列
   - 取前N-1项，其余合并为"其他"
   - 保持数据的可读性

2. **柱状图数据优化**:
   - 取前N项数据
   - 避免X轴过于密集
   - 保持图表清晰度

3. **字段名处理**:
   - 自动映射为中文友好标签
   - 下划线转换为空格
   - 首字母大写处理

---

## 错误处理

### HTTP状态码

| 状态码 | 含义 | 说明 |
|--------|------|------|
| `200` | 成功 | 图表配置生成成功 |
| `400` | 请求错误 | 输入数据格式或内容错误 |
| `500` | 服务器错误 | 内部处理异常 |

### 错误响应格式

```json
{
  "error": "错误信息描述",
  "code": "ERROR_CODE"
}
```

### 常见错误码

| 错误码 | 描述 | 解决方案 |
|--------|------|---------|
| `EMPTY_DATA` | 查询结果为空 | 确保查询返回有效数据 |
| `INVALID_DATA` | 输入数据格式错误 | 检查JSON格式和数据结构 |
| `MODEL_ERROR` | 模型内部错误 | 联系技术支持 |

---

## 使用示例

### 示例1: 部门员工统计

**请求**:
```bash
curl -X POST http://localhost:12345/generate-chart \
  -H "Content-Type: application/json" \
  -d '{
    "query_result": [
      {"department": "技术部", "employee_count": 186},
      {"department": "产品部", "employee_count": 125},
      {"department": "市场部", "employee_count": 89}
    ]
  }'
```

**响应**:
```json
{
  "type": "bar",
  "data": [
    {"department": "技术部", "employee_count": 186},
    {"department": "产品部", "employee_count": 125},
    {"department": "市场部", "employee_count": 89}
  ],
  "xKey": "department",
  "yKey": "employee_count",
  "xAxisLabel": "部门",
  "yAxisLabel": "员工数量",
  "supportedTypes": ["bar", "area"]
}
```

### 示例2: 月度销售趋势

**请求**:
```bash
curl -X POST http://localhost:12345/generate-chart \
  -H "Content-Type: application/json" \
  -d '{
    "query_result": [
      {"month": "2024-01", "sales": 150000},
      {"month": "2024-02", "sales": 180000},
      {"month": "2024-03", "sales": 220000}
    ]
  }'
```

**响应**:
```json
{
  "type": "area",
  "data": [
    {"month": "2024-01", "sales": 150000},
    {"month": "2024-02", "sales": 180000},
    {"month": "2024-03", "sales": 220000}
  ],
  "xKey": "month",
  "yKey": "sales",
  "xAxisLabel": "月份",
  "yAxisLabel": "销售额",
  "supportedTypes": ["area", "bar"]
}
```

### 示例3: 浏览器使用占比

**请求**:
```bash
curl -X POST http://localhost:12345/generate-chart \
  -H "Content-Type: application/json" \
  -d '{
    "query_result": [
      {"browser": "Chrome", "visitors": 275},
      {"browser": "Safari", "visitors": 200},
      {"browser": "Firefox", "visitors": 187},
      {"browser": "Edge", "visitors": 173},
      {"browser": "Opera", "visitors": 45},
      {"browser": "其他", "visitors": 120}
    ]
  }'
```

**响应**:
```json
{
  "type": "pie",
  "data": [
    {"browser": "Chrome", "visitors": 275},
    {"browser": "Safari", "visitors": 200},
    {"browser": "Firefox", "visitors": 187},
    {"browser": "Edge", "visitors": 173},
    {"browser": "其他", "visitors": 165}
  ],
  "nameKey": "browser",
  "valueKey": "visitors",
  "supportedTypes": ["pie", "radial"]
}
```

### 示例4: KPI总览

**请求**:
```bash
curl -X POST http://localhost:12345/generate-chart \
  -H "Content-Type: application/json" \
  -d '{
    "query_result": [
      {"total_revenue": 1285000, "avg_order_value": 450, "conversion_rate": 3.2}
    ]
  }'
```

**响应**:
```json
{
  "type": "stat",
  "data": [
    {"total_revenue": 1285000, "avg_order_value": 450, "conversion_rate": 3.2}
  ],
  "supportedTypes": null
}
```

---

## 服务部署

### 环境要求

- **Python版本**: 3.7+
- **依赖包**: FastAPI, Uvicorn, Pydantic

### 启动服务

```bash
# 安装依赖
pip install fastapi uvicorn pydantic

# 启动服务 (默认端口12345)
python chart_copy.py

# 或使用uvicorn
uvicorn chart_copy:app --host 0.0.0.0 --port 12345
```

### 环境变量配置

```env
# 服务配置
HOST=0.0.0.0
PORT=12345

# 日志级别
LOG_LEVEL=INFO
```

### Docker部署

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY chart_copy.py .
RUN pip install fastapi uvicorn pydantic

EXPOSE 12345

CMD ["uvicorn", "chart_copy:app", "--host", "0.0.0.0", "--port", "12345"]
```

---

## 性能特性

### 处理能力

- **并发处理**: 支持高并发请求
- **响应时间**: 平均 < 100ms (单次图表生成)
- **内存占用**: 轻量级，适合容器化部署

### 优化策略

1. **数据处理优化**:
   - 智能数据量限制
   - 内存高效处理
   - 避免不必要的数据复制

2. **算法优化**:
   - 线性时间复杂度检测
   - 高效的数据类型识别
   - 优化的排序算法

---

## 扩展指南

### 添加新图表类型

1. 在 `detect_chart_type` 方法中添加检测逻辑
2. 实现对应的数据处理方法
3. 更新响应模型和支持类型列表

### 自定义字段映射

```python
# 在 ChartGenerator.__init__ 中添加
self.field_mapping.update({
    'custom_field': '自定义字段标签',
    'your_field': '您的字段'
})
```

### 集成机器学习

可以扩展为基于机器学习的智能图表推荐系统，考虑更多数据特征和用户偏好。

---

## 版本历史

### v1.0.0 (当前版本)
- ✅ 基础图表类型支持 (bar, area, pie, radial, stat)
- ✅ 智能数据类型检测
- ✅ 自动标签生成
- ✅ 数据量优化处理
- ✅ 完整的错误处理机制
- ✅ RESTful API设计
- ✅ Swagger文档支持

### 计划功能
- 🔄 更多图表类型 (散点图、雷达图等)
- 🔄 自定义主题配置
- 🔄 数据缓存机制
- 🔄 批量处理接口
- 🔄 图表配置保存和恢复

---

## 技术支持

### 联系方式
- **问题反馈**: 通过GitHub Issues
- **技术文档**: API服务 `/docs` 端点
- **示例代码**: 完整的请求/响应示例

### 常见问题

**Q: 如何处理大量数据？**
A: API会自动限制显示项数，确保图表可读性和性能。

**Q: 支持哪些数据格式？**
A: 支持JSON格式的数组对象，字段可以是数值、字符串、日期等类型。

**Q: 如何自定义图表样式？**
A: 当前版本专注于数据结构分析，样式配置需要在客户端实现。

**Q: API的响应时间如何？**
A: 平均响应时间在100ms以内，支持高并发访问。

---

*文档最后更新: 2024-11-28*
*API版本: v1.0.0*